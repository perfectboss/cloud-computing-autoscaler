# Full 1,000-image ImageNet sample set, fetched separately.
# A 10-image subset is committed under sample-images/ for smoke tests.
test-images/

# Raw load-tester stdout (~1 MB per trial). The per-request data it contains
# is already extracted into results/<trial>/results.csv.
experiments/results/*/load_tester.log

# Python
__pycache__/
*.pyc
*.pyo

# Intermediate / scratch experiment artifacts (keep only results/)
experiments/results-*/
experiments/sanity/
experiments/smoke-check/
experiments/REPORT-*.pdf
slides_text.txt

# Load-tester scratch files
load-tester/smoke-*.csv
load-tester/smoke-*.txt

# OS cruft
Thumbs.db
.DS_Store
